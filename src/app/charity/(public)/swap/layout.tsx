export const metadata = {
  title: "SancSwap — SancCharity",
  description: "Swap BNB, USDT, BUSD, and SANC within the SancCharity ecosystem. Powered by PancakeSwap liquidity.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
