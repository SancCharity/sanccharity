export const metadata = {
  title: "Documentation — SancCharity",
  description: "Complete guide to donating, registering a charity, governance, staking, NFT receipts, and platform architecture on SancCharity.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
