export const metadata = {
  title: "SANC Tokenomics — SancCharity",
  description: "Token supply, distribution, tax breakdown, burn mechanics, and fee allocation for Sanctuary Token (SANC) on BNB Smart Chain.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
